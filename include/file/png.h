#pragma once

#include "file.h"
#include "../type/type.h"

namespace Base::PNG
{
    Int32 Read(const Char* filePath, Char* const output, const UInt64 outputSize)
    {


        return 1;
    }
}